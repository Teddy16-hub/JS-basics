function readText (input)
{
    let index = 0;
    while(true)
    {
        let str = input[index];
        if(str == 'Stop')
        {
            break;
        }
        console.log(str);
        index++;
    }
}
